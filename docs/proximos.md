1. Configurar credenciais reais: REDDIT_CLIENT_ID/SECRET, GCP key para BigQuery, GROQ_API_KEY
2. Rodar docker-compose up -d + ollama pull nomic-embed-text
3. Aplicar migration + seed niches
4. Disparar pipeline uma vez e abrir o dashboard